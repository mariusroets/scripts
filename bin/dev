#!/bin/bash

echo $1

dir=/data/code/$1
tmux new-session -A -s $1 -n $1 -c $dir nvim
#tmux split-window -t $1:$1.1 -h -l 50 -c $dir
#tmux split-window -t $1:$1.1 -v -f -l 15 -c $dir
#tmux bind v "split-window -t $1:$1.1 -h -l 50 -c $dir"
#tmux bind b "split-window -t $1:$1.1 -v -f -l 15 -c $dir"
#tmux bind v "break-pane -dP $1:$1.2 -n $1-right"
#tmux bind V "break-pane -dP $1:$1.3 -n $1-bottom"
#join-pane -hs dpowi:dpowi-right.1 -l 50 -t dpowi:dpowi.1

# Use this if there are other commands between new-session, and add -d to new-session
#tmux attach-session -t $1
